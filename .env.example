PORT=4000
JWT_SECRET=supersecretkey
CLIENT_ORIGIN=http://localhost:5173

# Optional: email notifications for admins on new orders.
# Leave SMTP_HOST unset to skip email entirely (WebSocket notification still works).
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
ADMIN_NOTIFY_EMAIL=admin@electric.com
